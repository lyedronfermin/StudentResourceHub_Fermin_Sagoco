
export interface Resource {
    title: string;
    description: string;
    category: string;
    url: string;
    author: string;
    publicationDate: Date;
    imageUrl?: string; // Optional image URL
  }